<!DOCTYPE html>
<html>
    <head>
        <title>Fabric Agency Database</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    </head>
    <body>

        {{> header}}

        <div class="container">
            {{{body}}}
        </div>

        {{> footer}}

        <script>
            function myFunction() {
                var input, filter, table1, table2, tr, td, i, txtValue;
                input = document.getElementById("myInput");
                filter = input.value.toUpperCase();
                table1 = document.getElementById("myTable1");
                table2 = document.getElementById("myTable2");
                
                // Adjust the table rows for the first table based on Name
                tr = table1.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[2]; // Index 2 corresponds to the Name column
                    if (td) {
                        txtValue = td.textContent || td.innerText;
                        if (txtValue.toUpperCase().indexOf(filter) > -1) {
                        tr[i].style.display = "";
                        
                        // Show corresponding rows in the second table based on Supplier ID
                        var supplierID = tr[i].getElementsByTagName("td")[1].textContent || tr[i].getElementsByTagName("td")[1].innerText;
                        showRowsInTable2(table2, supplierID);
                        } else {
                        tr[i].style.display = "none";
                        }
                    }
                }
            }

            // Function to show corresponding rows in the second table based on Supplier ID
            function showRowsInTable2(table, supplierID) {
                var tr, td, i, txtValue;
                tr = table.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[1]; // Index 1 corresponds to the Supplier ID column
                    if (td) {
                        txtValue = td.textContent || td.innerText;
                        if (txtValue === supplierID) {
                        tr[i].style.display = "";
                        } else {
                        tr[i].style.display = "none";
                        }
                    }
                }
            }

            function myFunction2() {
                var input, filter, table, tr, td, i, txtValue;
                input = document.getElementById("myInput");
                filter = input.value.toUpperCase();
                table = document.getElementById("myTable3");
                tr = table.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[5];
                    if (td) {
                    txtValue = td.textContent || td.innerText;
                    if (txtValue.toUpperCase().indexOf(filter) > -1) {
                        tr[i].style.display = "";
                    } else {
                        tr[i].style.display = "none";
                    }
                    }       
                }
            }

            function myFunction3() {
                var input, filter, table4, table5, table6, table7, table8, table9, table10, tr, td, i, txtValue;
                input = document.getElementById("myInput");
                filter = input.value.toUpperCase();
                table4 = document.getElementById("myTable4");
                table5 = document.getElementById("myTable5");
                table6 = document.getElementById("myTable6");
                table7 = document.getElementById("myTable7");
                table8 = document.getElementById("myTable8");
                table9 = document.getElementById("myTable9");
                table10 = document.getElementById("myTable10");
                
                // Adjust the table rows for the first table based on Name
                tr = table4.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[1]; // Index 1 corresponds to the Name column
                    if (td) {
                        txtValue = td.textContent || td.innerText;
                        if (txtValue.toUpperCase().indexOf(filter) > -1) {
                            tr[i].style.display = "";

                            // Show corresponding rows in the second table based on Supplier ID
                            var supplierID = tr[i].getElementsByTagName("td")[1].textContent || tr[i].getElementsByTagName("td")[1].innerText;
                            showRowsInTable3(table5, supplierID);
                            showRowsInTable4(table6, supplierID);
                            showRowsInTable5(table7, supplierID);
                            showRowsInTable6(table8, supplierID);
                            showRowsInTable7(table9, supplierID);
                            showRowsInTable8(table10, supplierID);
                        } else {
                            tr[i].style.display = "none";
                        }
                    }
                }
            }

            function showRowsInTable3(table5, supplierID) {
                var tr, td, i, txtValue;
                tr = table5.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[6]; // Index 6 corresponds to the Supplier ID column
                    if (td) {
                        txtValue = td.textContent || td.innerText;
                        if (txtValue === supplierID) {
                            tr[i].style.display = "";
                        } else {
                            tr[i].style.display = "none";
                        }
                    }
                }      
            }

            function showRowsInTable4(table6, supplierID) {
                var tr, td, i, txtValue;
                tr = table6.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[5]; // Index 6 corresponds to the Supplier ID column
                    if (td) {
                        txtValue = td.textContent || td.innerText;
                        if (txtValue === supplierID) {
                            tr[i].style.display = "";
                        } else {
                            tr[i].style.display = "none";
                        }
                    }
                }      
            }

            function showRowsInTable5(table7, supplierID) {
                var tr, td, i, txtValue;
                tr = table7.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[6]; // Index 6 corresponds to the Supplier ID column
                    if (td) {
                        txtValue = td.textContent || td.innerText;
                        if (txtValue === supplierID) {
                            tr[i].style.display = "";
                        } else {
                            tr[i].style.display = "none";
                        }
                    }
                }      
            }

            function showRowsInTable6(table8, supplierID) {
                var tr, td, i, txtValue;
                tr = table8.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[8]; // Index 6 corresponds to the Supplier ID column
                    if (td) {
                        txtValue = td.textContent || td.innerText;
                        if (txtValue === supplierID) {
                            tr[i].style.display = "";
                        } else {
                            tr[i].style.display = "none";
                        }
                    }
                }      
            }

            function showRowsInTable7(table9, supplierID) {
                var tr, td, i, txtValue;
                tr = table9.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[5]; // Index 6 corresponds to the Supplier ID column
                    if (td) {
                        txtValue = td.textContent || td.innerText;
                        if (txtValue === supplierID) {
                            tr[i].style.display = "";
                        } else {
                            tr[i].style.display = "none";
                        }
                    }
                }      
            }

            function showRowsInTable8(table10, supplierID) {
                var tr, td, i, txtValue;
                tr = table10.getElementsByTagName("tr");
                for (i = 0; i < tr.length; i++) {
                    td = tr[i].getElementsByTagName("td")[6]; // Index 6 corresponds to the Supplier ID column
                    if (td) {
                        txtValue = td.textContent || td.innerText;
                        if (txtValue === supplierID) {
                            tr[i].style.display = "";
                        } else {
                            tr[i].style.display = "none";
                        }
                    }
                }      
            }
            </script>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    </body>
</html>